apiVersion: v2
name: asafe-ai
description: A Safe Digital internal AI platform (forked from cgoinglove/better-chatbot)
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# App version — informational; the deployed image tag is values.image.tag.
appVersion: "1.26.0"
