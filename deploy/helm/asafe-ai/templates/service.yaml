apiVersion: v1
kind: Service
metadata:
  name: {{ include "asafe-ai.fullname" . }}
  labels:
    {{- include "asafe-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "asafe-ai.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
