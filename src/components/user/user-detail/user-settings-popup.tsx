"use client";

// Placeholder — full popup wired once appStore gains openUserSettings state.
export function UserSettingsPopup({
  userSettingsComponent: _userSettingsComponent,
}: {
  userSettingsComponent?: React.ReactNode;
}) {
  return null;
}
