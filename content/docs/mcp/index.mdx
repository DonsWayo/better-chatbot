---
title: MCP overview & transport policy
description: How asafe-ai connects to Model Context Protocol servers, and why cloud deployments are remote-only.
---

asafe-ai inherits upstream's first-class MCP support (`src/lib/ai/mcp/`) — client manager,
OAuth, tool loading into the chat loop — and adds the enterprise layer on top: a curated
[catalog](/docs/mcp/catalog) with team scoping, connection probing, and a strict transport
policy.

## Transport policy: cloud is remote-only

Like claude.ai on the web, **cloud deployments only allow remote MCP servers**
(HTTP/SSE URLs). Local `stdio` servers spawn arbitrary processes *on the server*, so they
are rejected at client-creation time with a clear message pointing users at the desktop
app.

The flag (`src/lib/const.ts`):

```ts
export const IS_MCP_SERVER_REMOTE_ONLY =
  IS_VERCEL_ENV || !(IS_DESKTOP_APP || MCP_ALLOW_LOCAL_SERVERS || IS_DEV);
```

| Context | stdio allowed? |
|---|---|
| Cloud / EKS production | **No** — remote-only. |
| Desktop app (`DESKTOP_APP=1`, set by the Electron build) | Yes — local tools run on the user's machine (see [Desktop](/docs/desktop)). |
| Local dev (`IS_DEV`) | Yes. |
| Self-hosted single-user opt-in (`MCP_ALLOW_LOCAL_SERVERS=true`) | Yes — trusted machines only. |

## Local (stdio) servers: governance plane

Where stdio is technically possible (desktop app, local dev, `MCP_ALLOW_LOCAL_SERVERS`),
a second, **policy** gate applies on top (ADR-0010 v1; default-deny per ADR-0009).
Local stdio servers stay off for everyone until an admin opts the org — or a team — in.

1. **Policy** (`src/lib/ai/mcp/local-policy.ts`) — layered org → team in
   `asafe_org_settings`, same pattern as the memory policy and model entitlements:
   - `local_mcp_enabled` — org base; **unset = FALSE** (default-deny).
   - `team_local_mcp_enabled:<teamId>` — team override (wins when set; no admin UI
     yet — set via `setTeamLocalMcpEnabled`).

   The org switch lives on **Admin → Feature flags → "Local MCP (desktop)"**; toggles
   are audit-logged (`admin_action`) and take effect immediately — the MCP manager's
   in-memory gate is flipped without restarting or deleting configs.
2. **Enforcement** — two layers:
   - *Save time* (`saveMcpClientAction`): a stdio config is rejected unless the saving
     user's org/team policy allows local MCP.
   - *Runtime* (`src/lib/ai/mcp/create-mcp-clients-manager.ts`): while the gate is off,
     stdio servers' tools are filtered out of `tools()` (never reach the model) and
     direct `toolCall`s are rejected. The manager is process-wide, so its gate resolves
     "org base OR any team override on"; per-team precision is enforced at save and
     arming time, where the user is known.
3. **Per-session consent (arming)** — a stdio server's first invocation in a server
   session requires arming: the connector detail page (Settings → Connectors →
   *connector*) shows an **"Enable local tools for this session"** button (stdio
   servers only, policy permitting, owner/admin only). Arming lasts 8 hours; unarmed
   calls return a clear error telling the user where to enable. Arming events are
   audit-logged. Per-**action** consent through the approvals/Inbox system is the v2
   follow-up (ADR-0010 gate 2).
4. **Audit** — every local stdio `toolCall` (allowed *and* denied) is written to
   `asafe_mcp_invocation_log` from the manager, attributed to the server owner with
   `mcp_server_id`, outcome, and duration. Chat-loop invocations are additionally
   audited by the chat route with the calling user (a known duplicate for stdio).

## Server config shape

`src/types/mcp.ts` defines two config variants (Zod-validated):

```ts
// Remote (the only kind allowed in cloud)
{ url: string, headers?: Record<string,string>,
  oauthClientId?: string, oauthClientSecret?: string }

// Stdio (desktop / dev only)
{ command: string, args?: string[], env?: Record<string,string> }
```

- **Static headers** cover token-style auth (e.g. `Authorization: Bearer …`).
- **OAuth/SSO**: when `oauthClientId`/`oauthClientSecret` are present the client performs
  a full OAuth handshake instead of relying on static headers — this is how per-user SSO
  on company connectors works, so the assistant inherits *the user's* permissions, never
  a service account's (the GDPR data-minimization default).

## Related knobs

| Env var | Effect |
|---|---|
| `NOT_ALLOW_ADD_MCP_SERVERS=1` | Disallow ad-hoc server additions entirely (catalog-only posture). |
| `MCP_MAX_TOTAL_TIMEOUT` | Max tool-call duration in ms (long-running tools). |
| `FILE_BASED_MCP_CONFIG=true` | File-based config storage backend instead of DB. |
| `REDIS_URL` | Optional — real-time MCP config sync across multiple app instances (polling otherwise). |

## Tool customizations

Users manage their personal MCP servers under **Settings → Connectors**
(`/settings/connectors`; the old `/mcp*` routes redirect here). Each connector row has an
inline editor for per-server and per-tool **instruction customizations**
(`/api/mcp/tool-customizations/[server]` and `…/[server]/[tool]`): extra prompt guidance
injected when that tool is loaded, e.g. "always answer with our Jira project key
conventions". Customizations are per-user and do not change the server's actual tool
schema.

## Per-tool controls

Admins (and owners of their own private servers) can switch **individual tools** of a
connector off without disconnecting the whole server — e.g. keep `search_issues` but turn
off `delete_repo`.

- **Where**: Settings → Connectors → *connector* — each tool row gets a switch (visible
  only to users who may manage the server; everyone else sees a read-only `off` badge).
- **Storage**: `mcp_server.disabled_tools` (jsonb array of tool names; `null`/`[]` means
  every tool is enabled). Toggled through the `setMcpToolEnabledAction` server action,
  which is gated by the same `canManageMCPServer` check as editing or deleting the server.
- **Enforcement** happens at the MCP manager layer (`src/lib/ai/mcp/create-mcp-clients-manager.ts`),
  not in the UI: disabled tools are filtered out of `tools()` so they never reach the
  model's tool list, and a direct `toolCall` against a disabled tool is rejected with
  *"Tool … is switched off for this connector by an administrator."* The gate is an
  in-memory set per server (O(1) per lookup), hydrated from the database on startup and
  refresh, and updated immediately when an admin flips a switch.

Per-tool controls are an *entitlement* (who may use what), distinct from the per-user
instruction customizations above and from the per-chat tool picker, which can only narrow
the already-entitled set.

Guardrails treat all MCP tool output as **untrusted input** — see
[Guardrails](/docs/governance/guardrails#untrusted-tool-output).
